using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

public class SaveJSON : MonoBehaviour
{
    //static public void SaveFileJson(object obj)
    //{
    //    File.WriteAllText(SoundManager.path, JsonUtility.ToJson(obj));
    //}

    //static public T LoadingJSON<T>()
    //{
    //    if (File.Exists(SoundManager.path))
    //    {
    //        return JsonUtility.FromJson<T>(File.ReadAllText(SoundManager.path));
    //    }
    //    else return default(T);
    //} 
}
